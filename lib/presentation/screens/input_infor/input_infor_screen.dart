import 'package:flutter/material.dart';

class InputInforScreen extends StatelessWidget {
  const InputInforScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
